/** Written to `upminer_results.dossiers_data` when rows are stored in relational tables. */
export const UPMINER_RELATIONAL_DOSSIERS_DATA_MARKER = {
  storedRelationally: true,
  schemaVersion: 1,
} as const;
